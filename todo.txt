improve storage structures
performance
reduce memory usage
git grep -in fixme

zcat
fwlite
use bcn in event maps
dq vs time (mismatched evn, frac0, nmatched?)
event prescale
bcn vs. fed
fine bx binning

auto SOI
restore cron
chainI = chain if files2 == files1 else tchain(xxyy)

